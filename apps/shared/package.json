{
  "name": "@brick/shared",
  "main": "index.js",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "volta": {
    "extends": "../../package.json"
  }
}